server:
  port: 8002
spring:
  application:
    name: cloud-alibaba-consumer
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
# 开启Feign对Sentinel的支持
feign:
  sentinel:
    enabled: true
