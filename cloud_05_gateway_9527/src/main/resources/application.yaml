server:
  port: 9527

spring:
  application:
    name: cloud-gateway # 以微服务注册进入到consul中
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}
        prefer-ip-address: true